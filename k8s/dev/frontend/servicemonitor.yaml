apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: deeppython-frontend
  namespace: storefrontdevelopment
  labels:
    app.kubernetes.io/name: deeppython-frontend
    app.kubernetes.io/managed-by: deepagent
    release: monitoring
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: deeppython-frontend
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
