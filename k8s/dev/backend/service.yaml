apiVersion: v1
kind: Service
metadata:
  name: deeppython-backend
  namespace: first-dev
  labels:
    app.kubernetes.io/name: deeppython-backend
    app.kubernetes.io/managed-by: deepagent
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: deeppython-backend
  ports:
    - name: http
      port: 80
      targetPort: 8000
